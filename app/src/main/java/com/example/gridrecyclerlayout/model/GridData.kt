package com.example.gridrecyclerlayout.model


class GridData {

    var icons:Int ? = 0
    var iconText:String ? = null

    constructor(icons: Int?, textText: String?) {
        this.icons = icons
        this.iconText = textText
    }
}